{
  "name": "sora-art-source",
  "private": true,
  "description": "Root-level tooling for 空を歩く、光を編む: the capture/measure loop (scripts/) and the asset builder (scripts/puddle.js). The app itself lives in app/.",
  "dependencies": {
    "playwright": "^1.62.1",
    "sharp": "^0.35.3"
  }
}
